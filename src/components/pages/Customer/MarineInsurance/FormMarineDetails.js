import React, { useState,useEffect} from 'react';

const FormMarineDetails = ({ formData, setFormData, nextStep, prevStep, props })  =>{
  console.log('formData', formData);
  const classes = useStyles();
  co

}